Order these films by how well each one answers the request. Judge only the text you
are given. You are not recommending, you are sorting.

Request: {query}

Candidates, one per line, written as `label. description`:

{candidates}

Reply with every label exactly once, best match first, separated by commas.
No other text, no explanation, no numbering of your own.
